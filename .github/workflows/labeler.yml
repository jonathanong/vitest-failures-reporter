name: Labeler

on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-slim
    timeout-minutes: 5
    steps:
      - uses: actions/labeler@v6.1.0
